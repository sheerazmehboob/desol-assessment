'use client'
import CarSubmissionForm from "../components/CardSubmissionForm";

const CarSubmissionPage = () => {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-100">
            <CarSubmissionForm />
        </div>
    );
};

export default CarSubmissionPage;
